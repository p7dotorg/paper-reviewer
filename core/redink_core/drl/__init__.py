"""dataset research loop — a second graph that scans dataset sources, scores
opportunity, and writes an OKF knowledge bundle. Shares the LangGraph runtime,
model factory and TUI with the paper reviewer, but is a distinct domain."""
